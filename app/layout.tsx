import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const _geist = Geist({ subsets: ["latin"] });
const _geistMono = Geist_Mono({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: 'Professional Truck Driver Services | Reliable Logistics Solutions',
  description: 'Professional truck driver with years of experience providing reliable freight and logistics services. Get a quote today for your transportation needs.',
  keywords: 'truck driver, freight, logistics, transportation, hauling services',
  authors: [{ name: 'Your Truck Driver Business' }],
  viewport: {
    width: 'device-width',
    initialScale: 1,
    maximumScale: 1,
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className="font-sans antialiased">
        {children}
        <Analytics />
      </body>
    </html>
  )
}
